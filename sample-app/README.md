# sample-app

A deliberately imperfect module used to exercise ZeroBug end to end. It has a spec
(`docs/pricing-rules.md`), tests that pass, and a real defect the tests do not cover.

```bash
cd sample-app && node --test
```
